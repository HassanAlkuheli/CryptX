package com.cryptx.cryptx.ui.theme

import androidx.compose.ui.graphics.Color

// Primary Blue Palette
val Primary = Color(0xFF1E88E5)        // Main Blue
val PrimaryVariant = Color(0xFF1565C0) // Darker Blue

// Secondary (accent / highlights)
val Secondary = Color(0xFFFFC107)      // Amber / gold-ish like Binance

// Background
val Background = Color(0xFF121212)     // Dark mode style
val Surface = Color(0xFF1E1E1E)        // Cards / panels

// Text / On-Colors
val OnPrimary = Color.White
val OnBackground = Color.White
val OnSurface = Color.White

// Error
val Error = Color(0xFFD32F2F)          // Red for errors